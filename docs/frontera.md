1. Monté un hook `Stop` de Claude Code en `blog-api/.claude/settings.json` que ejecuta `.claude/hooks/registro-guardia.sh`: revisa con git blog-api y `../blog-ai`, impide que el agente dé la tarea por terminada (exit 2) si hay archivos `.ts` o `.py` cambiados sin su línea en el `REGISTRO.md` de ese repo, y deja una línea en `blog-api/tmp/guardia.log` cada vez que se ejecuta.

2. Dejó de funcionar en el momento en que lancé la sesión desde blog-ai con el mismo encargo: el agente modificó `post_service.ts` (en blog-api) e `indice.py` y dio la tarea por terminada sin ningún `REGISTRO.md` y sin ningún aviso, porque el hook se carga desde el proyecto donde arranca la sesión y no desde el repo del archivo editado; lo comprobé yendo a mirar a propósito: `blog-api/tmp/guardia.log` siguió en 11 líneas (desde blog-api había pasado de 8 a 10), `git status` mostraba el código cambiado sin `REGISTRO.md` en ningún repo y el transcript de la sesión no registra ningún bloqueo del hook.
